import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard | CPCLLC",
  description: "Multi-service app",
};

import CompanyDashboard from "@/screens/dashboard/company";

export default CompanyDashboard;
