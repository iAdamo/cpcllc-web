import type { Metadata } from "next";
import SignUpPage from "@/screens/auth/SignUpPage";

export const metadata: Metadata = {
  title: "Sign up — CompaniesCenter",
  robots: { index: false, follow: false },
};

export default SignUpPage;
