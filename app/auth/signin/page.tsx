import type { Metadata } from "next";
import SignInPage from "@/screens/auth/SignInPage";

export const metadata: Metadata = {
  title: "Sign in — CompaniesCenter",
  robots: { index: false, follow: false },
};

export default SignInPage;
