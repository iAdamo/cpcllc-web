import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Update Service | CPCLLC",
  robots: {
    index: false,
    follow: false,
  },
};

import CreateService from "@/screens/profile/ServiceUpdate/CreateService";

export default CreateService;
